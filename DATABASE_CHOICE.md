# Database choice  

**Cosmos DB**: is the most suitable DB for JSON files because it's a JSON native document, it automatically indexes every property in every JSON document, COSMOS db is built for Serverless.
